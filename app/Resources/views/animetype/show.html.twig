{% extends 'base.html.twig' %}

{% block body %}
    <h1>Animetype</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animeType.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ animeType.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('animetype_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
