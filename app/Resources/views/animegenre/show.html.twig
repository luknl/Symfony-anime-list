{% extends 'base.html.twig' %}

{% block body %}
    <h1>Animegenre</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animeGenre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ animeGenre.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('animegenre_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('animegenre_edit', { 'id': animeGenre.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
